# Django
SECRET_KEY=your-secret-key
DEBUG=false
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL
DB_NAME=mydb
DB_USER=myuser
DB_PASSWORD=mypass
DB_HOST=localhost
DB_PORT=5432

# Production only: comma-separated origins for CORS
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
